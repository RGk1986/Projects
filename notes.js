// A simple function that calls an alert on the name variable. 

//   const names = ['Rob', 'James', 'Dave', 'Ania']

//   function showAlert(){
//     alert(names[0] + ' ' + names[1] + ',you have been alerted!')
//   }
// showAlert()

// A simple arrow function to utilise the forEach functionality on the array 'names'
 
// names.forEach(name =>{
//   console.log(name + ' is the best!')
// })

